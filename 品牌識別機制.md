---
title: 品牌識別機制
permalink: /brand-positioning/
---

# 產品前提與核心價值主張
樹享券 2.0 的核心定位是：*讓用戶的點數有更多日常場景可以自動去化*，並提供接近「即時折抵刷卡金」的消費體驗
品牌通路的合作誘因：來自於背後的刷卡客戶群，屬信用卡通路回饋的延伸合作，所以是否需要設計為「以品牌為主」的點數效益放大體驗，有討論空間？

# 方案 A：神坊自建品牌識別規則
## 產品設計方向
用戶主動選擇特定品牌（如全家、康是美），系統在對應品牌的刷卡交易發生時，自動以點數兌換消費券即時折抵
神坊在後台為每個合作品牌維護一份識別規則（以 mcc_code、mid 範圍、merchant_name 的條件組合），比對每筆交易屬於哪個品牌，再觸發清算邏輯

## 價值主張
- 對用戶：點數在指定品牌自動發揮，體驗直覺、折抵即時 
- 對品牌：可設計差異化的兌換規則（如「滿 100 折 21」），具備品牌識別度 
- 對神坊：完整掌控系統邏輯，可在不經銀行協作前提下，快速配合品牌端調整規則

## 主要風險
- 品牌識別依賴 merchant_name 模糊比對與 mid 範圍設定，因不同收單行的 mid 可能重複，識別準確性存在先天限制
- 這套識別規則有兩個剛需：
1. 需由工程師配合品牌通路定期維護
2. 依賴品牌主動提供門市資料
其中任一項不符，會讓此模式無法往下維護

## YAML 複合式品牌識別規則構想
> 本節為內部討論稿，用於評估若由神坊維護品牌識別規則時，可能採用的設定檔方向；尚未定案為正式 PRD、API 規格或資料庫設計。

### 設計方向
- 神坊維護一份 YAML 品牌識別規則，並以 Git 版本控管，透過 code review、測試與部署流程控管規則變更。
- 在刷卡交易進入清算前，由前置 brand resolver 讀取交易中的 `mid`、`mcc_code`、`merchant_name`，依 YAML 規則解析出 `brand_id`。
- `create_order` 清算流程仍以 `brand_id` 作為輸入；本階段不調整主要 PRD、API 欄位、資料庫 schema 或正式錯誤碼。
- YAML 規則需支援三個欄位的複合式條件，包括 AND、OR、巢狀條件，以及 `mid`、`mcc_code`、`merchant_name` 的不同比對方式。
- `merchant_name` 可單獨命中品牌，不強制搭配 `mid` 或 `mcc_code`。
- `merchant_name` 的 `fuzzy` 比對不在 YAML 暴露 threshold 概念；模糊比對細節由 resolver 內部固定管理，規則維護者只維護可接受的品牌名稱或別名。
- 若未命中任何品牌，或同一筆交易跨品牌多重命中，系統不觸發自動折抵，以降低誤判後錯扣點與代償的風險。

### YAML 範例
```yaml
version: "2026-06-03.001"

normalization:
  merchant_name:
    trim_spaces: true
    full_width_to_half_width: true
    uppercase_ascii: true
    remove_symbols: true

brands:
  - brand_id: "BRAND_FAMILYMART"
    enabled: true
    rules:
      - rule_id: "familymart_name_001"
        enabled: true
        when:
          field: "merchant_name"
          op: "fuzzy"
          value:
            - "全家"
            - "全家便利商店"
            - "FAMILYMART"

      - rule_id: "familymart_mcc_name_001"
        enabled: true
        when:
          all:
            - field: "mcc_code"
              op: "in"
              value:
                - "5411"
                - "5499"
            - field: "merchant_name"
              op: "contains"
              value: "全家"

      - rule_id: "familymart_mid_range_001"
        enabled: true
        when:
          any:
            - all:
                - field: "mid"
                  op: "range"
                  value:
                    - "1234000000"
                    - "1234999999"
                - field: "mcc_code"
                  op: "eq"
                  value: "5411"
            - field: "mid"
              op: "prefix"
              value: "98765"
```

### 初步比對原則
- `all` 表示所有子條件皆需成立；`any` 表示任一子條件成立即可。
- `mcc_code` 可支援 `eq`、`in`。
- `mid` 可支援 `eq`、`in`、`prefix`、`range`。
- `merchant_name` 可支援 `eq`、`contains`、`regex`、`fuzzy`。
- 同一品牌命中多條規則時，仍視為該品牌命中。
- 命中 0 個品牌時，不觸發自動折抵。
- 命中 1 個品牌時，brand resolver 輸出該 `brand_id`，後續再進入既有清算流程。
- 命中超過 1 個品牌時，視為跨品牌衝突，不用優先序或分數決勝，該筆交易不觸發自動折抵，待後續調整規則。

---

# 方案 B：由發卡主機（國泰世華）帶入 brand_id
## 產品設計方向
產品體驗與方案 A 相同，差異在技術架構：
- 品牌識別的工作交由發卡主機（國泰世華）在銀行端完成
- 建立訂單時直接帶入已識別的 brand_id，神坊不再自行比對
- 現 CUBE 計算回饋，已有維護此類資料源
  - 進一步發展成功能或 API 即可使用（推論）

## 價值主張
- 對用戶：折抵體驗不變 
- 對銀行：掌握品牌識別的技術主導權，可依銀行資料做更準確的比對 
- 對神坊：系統複雜度降低，移除比對邏輯 

## 主要風險
需銀行配合開發新的品牌識別邏輯並開放 API 介面，開發意願與排期不由神坊控制
一旦比對邏輯出錯，問題排查需仰賴銀行與品牌端協作，掌握度低
此方案能否推進，以銀行是否願意配合為前提
