---
title: 樹享券 2.0 PRD
permalink: /prd/
---

# 樹享券 2.0 PRD

Product Requirements Document  
（內部工程版）

版本：1.0  
建立日期：2026/06/06  
發行單位：神坊

---

## 一、專案背景

原規劃方案「點數 + 金流整合支付（點加金）」因法規限制暫緩，本次改以「券加金」模式推動，即樹享券 2.0。

核心機制：用戶以點數兌換由神坊發行的「消費券（coupon）」，並於刷卡時即時折抵消費金額，在定性上趨近於消費代金，與一般禮票券有所區別。

## 二、名詞定義

| 名詞 | 定義 |
| --- | --- |
| `brand` | 合作的品牌通路，亦稱為特店（例：全家、7-11、康是美） |
| `campaign` | 對應 `brand` 的產券邏輯，定義兌換規則 |
| `coupon` | 基於 `campaign` 產出、所屬於特定用戶的 instance |
| `coupon_wallet` | 用戶的券夾，存放所有 `available` / `processing` 的券 |
| `point_balance` | 用戶目前可用於兌換 `coupon` 的點數餘額 |

## 三、Coupon 狀態機

| 狀態 | 說明 | 可觸發的轉換 |
| --- | --- | --- |
| `available` | 可使用，存在於用戶券夾 | `processing`（成立訂單）<br>`expired`（到期） |
| `processing` | 交易授權中，等待商戶請款 | `completed`（請款完成）<br>`available`（取消且未到期）<br>`expired`（取消時已到期） |
| `completed` | 核銷完成，神坊代償完畢 | 終態 |
| `expired` | 超過有效期限自動過期 | 終態 |

> 注意：`processing` 狀態的 `coupon` 在後續新訂單計算時不可再被使用，僅 `available` 的 `coupon` 可納入折抵計算。

## 四、Brand / Campaign 規則欄位

### 4.1 Brand 欄位

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `brand_id` | String | `brand` 主鍵，用於識別品牌主資料。 |
| `brand_name` | String | 品牌名稱，供前台顯示與後台維護使用。 |
| `brand_logo` | String | 品牌 Logo 資源位置或識別值，供前台品牌展示使用。 |
| `brand_category` | String | 品牌分類，用於品牌分組、顯示或營運管理。 |
| `treepoint_merchant_provider_key` | String | 該 `brand` 對應的 TreePoint 用點通路識別。執行 `create_order` 扣點時，系統需依 `brand` 讀取此欄位並帶入點數帳務流程，以區分點數是用於全家換券、7-11 換券等不同品牌場景。此欄位為品牌主資料必填，DB 不可為 `null`。 |
| `is_enabled` | Boolean | 表示品牌主資料是否啟用；此欄位不代表該品牌目前是否有 active campaign。 |
| `created_at` | DateTime | `brand` 建立時間（UTC+8 ISO 8601）。 |
| `updated_at` | DateTime | `brand` 最後更新時間（UTC+8 ISO 8601）。 |

### 4.2 Campaign 欄位

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `campaign_id` | String | `campaign` 主鍵。 |
| `brand_id` | String | 對應 `brand` 主鍵，表示該 `campaign` 隸屬哪一個品牌。 |
| `campaign_name` | String | `campaign` 名稱，供後台辨識與營運維護使用。 |
| `unit_cash_amount` | Integer | 【門檻】每消費滿 N 元，可對應使用一張 `coupon` |
| `unit_point_amount` | Integer | 兌換一張 `coupon` 所需點數 |
| `unit_discount_amount` | Integer | 一張 `coupon` 可折抵的金額（元） |
| `max_redeem_count` | Integer | 單筆交易中，當前 active campaign 最多可使用幾張券 |
| `start_at` | DateTime | `campaign` 生效起始時間（UTC+8 ISO 8601） |
| `end_at` | DateTime | `campaign` 生效結束時間（UTC+8 ISO 8601） |
| `created_at` | DateTime | `campaign` 建立時間（UTC+8 ISO 8601）。 |
| `updated_at` | DateTime | `campaign` 最後更新時間（UTC+8 ISO 8601）。 |

每一 `brand` 底下同一時間只會有一個「自動兌換」的 active campaign。`active campaign` 的判斷方式為：當前時間落在 `start_at` 與 `end_at` 之間。用戶需事先同意「刷卡時自動以點數兌換」，系統才會在刷卡時觸發清算。

### 4.3 全域參數

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `coupon_valid_days` | Integer | 全域 coupon 有效天數設定，不屬於 `campaign` 欄位。發券時需以 `issued_at` 所在 UTC+8 日期加上此天數後，取當日 `23:59:59.999` 作為 `coupon.expired_at`。 |
| `brand_selection_limit` | Integer | 全域可選品牌數量上限，用於限制單一用戶最多可設定幾個品牌為自動兌換品牌。 |
| `brand_change_limit_per_month` | Integer | 全域每月品牌異動次數上限，用於限制單一用戶每月可進行幾次品牌增刪或調整。 |

## 五、清算邏輯（create_order）

### 5.1 觸發條件

- 用戶已選擇該 `brand` 為自動兌換品牌
- 用戶已完成點數授權，且神坊系統可取得或驗證該授權結果
- 用戶已同意在此品牌刷卡時自動兌換
- 用戶的 `coupon_wallet` 有 `available` 的券，或 `point_balance > 0`

> 注意：即使 `point_balance = 0`，只要 `coupon_wallet` 仍有 `available` 的券，仍應觸發 `create_order`。

### 5.2 計算步驟

1. 取出用戶在此 `brand` 下所有 `available coupon`，排除已過期券後，依下列順序排序（FIFO）：

   ```text
   expired_at ASC -> created_at ASC -> coupon_id ASC
   ```

2. 逐張檢查排序後的 `available coupon`：

   ```text
   若 剩餘消費額 >= coupon.unit_cash_amount，則使用該券
   若 剩餘消費額 < coupon.unit_cash_amount，則跳過該券，繼續檢查下一張
   ```

   每張券要嘛整張使用，要嘛不使用，不做部分用券。
   
   若該舊券 `campaign_id` 對應當前 active campaign，則需額外檢查本次已使用的 active-campaign 券數是否已達 `max_redeem_count`；若已達上限，該舊券需跳過。

   若該舊券屬於歷史 campaign，則不受 `max_redeem_count` 限制。

3. 計算「未被佔用消費額」：

   ```text
   剩餘消費額 = 原始消費額 - Σ（已使用券的 unit_cash_amount）
   ```

4. 在剩餘消費額上套用 active campaign：

   ```text
   可用張數 = 剩餘消費額 // unit_cash_amount
   ```

5. 先計算本次已使用的 active-campaign 舊券張數；再取 `remaining_active_campaign_quota = max_redeem_count - active_campaign_coupon_used_count`，最後以 `min(可用張數, point_balance // unit_point_amount, remaining_active_campaign_quota)` 決定本次可再新發幾張 active-campaign 券。若 `remaining_active_campaign_quota <= 0`，則本次不得再新發任何 active-campaign 券。
6. 執行點數扣除，並即時派發對應張數的新 `coupon`（狀態為 `processing`）。每張新券建立時即寫入固定 `expired_at`：

   ```text
   expired_at = (issued_at 所在 UTC+8 日期 + coupon_valid_days) 的 23:59:59.999
   ```

7. 所有被使用的既有 `available coupon` 狀態改為 `processing`。
8. 回傳本次折抵總金額：

   ```text
   本次折抵總金額 = Σ（本次所有 processing coupon 的 unit_discount_amount）
   ```

### 5.3 create_order 成功定義

- `create_order` 僅在同一個 DB transaction 內完成以下動作後，才視為正式成功建立：
  - 完成點數扣除
  - 完成新券派發
  - 完成既有 `available coupon` 轉為 `processing`
  - 完成 order 建立
  - 完成 `order_logs` 建立
- 任一環節失敗，整筆 transaction 應 rollback，該次 `create_order` 視為訂單未成功建立。
- 執行點數扣除時，系統應依訂單所屬 `brand` 讀取其 `treepoint_merchant_provider_key`，作為點數帳務通路識別。
- order 僅保存最終 `brand_id`，不額外保存其他門店層原始交易識別欄位。

### 5.4 計算範例

情境設定：

| 項目 | 數值 |
| --- | --- |
| 訂單金額（`cash_amount`） | 620 元 |
| 用戶 `point_balance` | 26 點 |
| active campaign | 滿 100 折 21：`unit_cash_amount=100`、`unit_point_amount=20`、`unit_discount_amount=21`、`max_redeem_count=3` |
| `coupon_wallet` 中既有券 | `coupon_01`（old_campaign）：`unit_cash_amount=400`、`unit_discount_amount=120` |

計算過程：

1. FIFO 取出 `coupon_01`（唯一 `available` 券）。
2. 計算剩餘消費額：

   ```text
   620 // 400 = 1
   coupon_01 可使用 1 張（剛好符合門檻）

   剩餘消費額 = 620 - 400 = 220
   ```

3. 套用 active campaign：

   ```text
   220 // 100 = 2
   理論可再用 2 張
   ```

4. 確認點數是否足夠：

   ```text
   26 // 20 = 1
   點數只夠換 1 張

   campaign 上限為 3 張

   min(2, 1, 3) = 1
   ```

5. 扣點 20，派發 `coupon_02`（`processing`）。
6. `coupon_01` 狀態改為 `processing`。
7. 折抵總額：

   ```text
   120 + 21 = 141 元
   ```

### 5.5 帳務流程

1. 用戶授權商戶刷卡 620 元。
2. 神坊扣除用戶點數 20 點。
3. 商戶向銀行請款 620 元。
4. 神坊確認核銷 `coupon_01` + `coupon_02`。
5. 神坊代償 141 元給銀行。
6. 用戶實際應繳卡費：`620 - 141 = 479` 元。

### 5.6 重複請求處理規則

- `create_order` 以 `order_id` 作為唯一訂單識別。
- 同一 `order_id` 已成功建立後，任何再次收到的 `create_order` 請求，一律回 `ORDER_ALREADY_EXIST`。
- 不區分再次收到的請求內容是否與首次一致；即使 `brand`、`cash_amount` 或其他欄位不同，仍回 `ORDER_ALREADY_EXIST`。
- 重複建立不得再次扣點、發券、改券狀態或新增事件。
- 首次 `create_order` 成功後，訂單即進入 `PROCESSING` 狀態，等待後續 `finalize_order`。

## 六、訂單生命週期

| 事件 | 觸發方 | Coupon 狀態變化 | 點數 |
| --- | --- | --- | --- |
| `create_order`<br>（刷卡授權後） | 發卡主機 | `available` → `processing` | 即時扣除 |
| `finalize_order`<br>（商戶請款完成） | 發卡主機 | `processing` → `completed` | 不異動 |
| `finalize_order`<br>（商戶取消交易） | 發卡主機 | `processing` → `available`（未到期）<br>`processing` → `expired`（已到期） | 不退還 |

> 注意：`create_order` 與 `finalize_order` 為非同步，兩者之間可能有數天時間差。`processing` 期間的券不可再被計入下一筆交易的折抵計算。

> 注意：取消交易後，本次交易即時兌換產生的新券（如 `coupon_02`）若在 finalize 當下尚未到期，則回 `available`；若已到期，則回 `expired`。點數不返還。

### 6.1 訂單狀態

| order_status | 說明 |
| --- | --- |
| `PROCESSING` | 已完成 `create_order`，等待商戶請款或取消交易結果 |
| `COMPLETED` | 已收到請款完成的 `finalize_order`，訂單結案 |
| `CANCELLED` | 已收到取消交易的 `finalize_order`，訂單結案 |

- `PROCESSING` 是唯一可執行 `finalize_order` 的狀態。
- `COMPLETED`、`CANCELLED` 為終態，進入後不得再次轉換。

### 6.2 重複 finalize 規則

- 同一 `order_id` 已完成 `finalize_order` 後，再收到任何 `finalize_order` 請求，一律回 `ORDER_ALREADY_FINALIZED`。
- 不區分再次收到的是相同結果或不同結果；已 `COMPLETED` 後收到 `CANCELLED`，或已 `CANCELLED` 後收到 `COMPLETED`，皆回 `ORDER_ALREADY_FINALIZED`。
- 重複 `finalize_order` 不得再次改變訂單狀態、券狀態、點數或新增事件。

## 七、資訊流程

### Flow 1：品牌一覽

用戶可瀏覽所有合作品牌及對應的當期自動兌換規則。

需回傳欄位：

- `brand_name`
- `brand_logo`
- `brand_category`
- `brand_active_campaign_details`（`unit_cash_amount`、`unit_point_amount`、`unit_discount_amount`、`max_redeem_count`）

### Flow 2：用戶授權

用戶需先同意樹享券平台可使用其點數，系統需能取得或驗證該授權結果，作為後續品牌設定與 `create_order` 用點清算的前置條件。

需確認或接收欄位：

- `user_id`
- `is_authorized`
- `authorized_at`
- `terms_version`

### Flow 3：選擇品牌

用戶選擇偏好的 `brand` 加入自動兌換清單。

系統需驗證：

- 用戶是否已完成點數授權。
- 用戶是否可更換：每自然月限一次（1 日重置），為環境參數控制。
- 用戶已選品牌數量：每人上限 3 個，為環境參數控制。
- 所選 `brand` 是否有 active campaign。

### Flow 4：瀏覽已選品牌

顯示用戶目前的品牌設定狀態，包含服務是否啟用，以及已選擇的品牌清單及對應 campaign 詳情。

需回傳欄位同 Flow 1（`brand_name`、`brand_logo`、`brand_category`、`brand_active_campaign_details`）。

另需回傳：

- `auto_redeem_enabled`

若用戶處於暫停用券狀態，仍可回傳目前已選且當前有 active campaign 的品牌清單，但 `auto_redeem_enabled = false`。

### Flow 5：異動紀錄

調閱用戶過往 1 年內的品牌選擇異動紀錄，包含：

- 異動時間
- 異動行為（首次啟用、暫停用券、重啟用券、更換品牌）
- 更換品牌需記錄「更換前的品牌清單」vs「更換後的品牌清單」

底層資料模型採 `brand_change_logs` 單表事件模型：

- 同一個 `request_id` 代表同一次品牌異動操作
- 初次選牌時，同批次可寫入多筆 `INITIAL_SELECTION`
- 一般品牌異動時，同批次可寫入多筆 `ADD_BRAND` / `REMOVE_BRAND`
- `PAUSE` / `RESUME` 為單筆事件，且不關聯特定 `brand_id`

### Flow 6：券夾

顯示用戶券夾列表，預設回全部券狀態，並可依品牌或券狀態篩選。

需回傳欄位：

- `coupon_id`
- `status`
- `brand_id`
- `brand_name`
- `brand_logo`
- `campaign_id`
- `campaign_name`
- `unit_cash_amount`
- `unit_point_amount`
- `unit_discount_amount`
- `max_redeem_count`
- `expired_at`

### Flow 7：刷卡

子流程 7a：`create_order`（信用卡授權後同步執行）

- 由發卡主機呼叫，帶入 `brand`、用戶識別、消費金額，以及本次刷卡卡號後四碼。
- 神坊執行第五章清算邏輯，回傳折抵金額。
- 若同一 `order_id` 再次建立，一律回 `ORDER_ALREADY_EXIST`，不重做清算。

訂單底層資料模型包含：

- `order_logs`：保存 `CREATED`、`COMPLETED`、`CANCELLED` 等訂單歷程事件
- `order_coupon_items`：保存本次訂單實際使用的券明細快照

> 卡號後四碼僅作為訂單顯示資訊保存，不參與任何品牌識別、點數清算或折抵計算邏輯。

子流程 7b：`finalize_order`（商戶請款或取消交易後非同步執行）

- 請款成功：`coupon` 狀態 `processing` → `completed`，神坊代償。
- 取消交易：`coupon` 狀態依是否已到期轉為 `available` 或 `expired`，點數不退還。
- 若該 `order_id` 已完成最終化，再收到任何 `finalize_order`，一律回 `ORDER_ALREADY_FINALIZED`。

## 八、角色與系統邊界

| 角色 | 說明 |
| --- | --- |
| 神坊（票券發行方） | 負責 `coupon` 發行、點數清算、代償銀行 |
| 發卡主機（銀行） | 觸發 `create_order` 與 `finalize_order`，為主要呼叫方 |
| 品牌通路（特店） | 刷卡場域，向銀行請款或申請取消交易 |
| 樹享券平台前台端 | 用戶介面，顯示品牌清單、券夾、異動紀錄 |

## 九、商務規則彙整（環境參數）

| 規則 | 預設值 | 備註 |
| --- | --- | --- |
| 每人最多選擇品牌數 | 3 個 | 環境參數，可調整 |
| 品牌更換頻率 | 每自然月 1 次 | 以日曆月計算，1 日重置 |
| Coupon 使用順序 | FIFO（先到期先用） | 依 `expired_at`、`created_at`、`coupon_id` 排序；不符合門檻的券跳過 |
| 點數不足時 | 只換點數足夠支付的張數 | 不部分扣點 |
| 取消交易時點數 | 不返還 | 券未到期回 `available`；已到期回 `expired` |
| `processing` 券可用性 | 不可納入下一筆交易計算 | 視同已用 |
| `coupon_valid_days` | 全域參數 | 發券時計算 `expired_at = (issued_at 所在 UTC+8 日期 + coupon_valid_days) 的 23:59:59.999` |

---

文件版本：1.0  
建立日期：2026/06/06  
發行單位：神坊
