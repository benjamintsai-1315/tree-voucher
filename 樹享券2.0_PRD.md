---
title: 樹享券 2.0 PRD
permalink: /prd/
---

# 樹享券 2.0 PRD

Product Requirements Document  
（內部工程版）

版本：1.0  
建立日期：2026/06/06  
發行單位：神坊

---

## Changelog

| Date | Summary |
| ---- | ------- |
| 2026-07-01 | §4.1/§4.2 `brand_id`/`campaign_id` 欄位說明補上 ULID 型別註記（對應 database-schema.md `brands.id` 改為 ULID） |
| 2026-07-01 | `member_authorize`/`member_unauthorize` 改名為 `activate_member`/`deactivate_member`；response 欄位 `auth_status` 改為 `status`，值 `AUTHORIZED`/`DEAUTHORIZED` 改為 `ACTIVE`/`INACTIVE`（`members.auth_status` 資料庫欄位與 enum 維持不變，僅 API 層轉換） |
| 2026-06-25 | `update_member_settings` 拆分為 `update_member_selected_brands`（品牌清單全量取代）與 `update_member_auto_redeem_settings`（暫停／啟用）；`member_brand_change_logs` 補回 `type` 欄位，支援 pause/resume 寫入（`data: null`）；`data` 格式改為快照模式（`before_brands` / `after_brands`，非 diff），API layer 可直接使用；Flow 4 改為回傳 `selected_brand_ids`（移除完整品牌物件）、`last_brand_selection_changed_at`（移除 `max_selectable_brand_count`）；Flow 5 補充 type enum 與 pause/resume 寫入規則；過時 API 名稱、欄位名全面修正（`rotation_key` → `rotation_id`、`order_coupon_items` → `order_coupon_logs`、`get_member_selected_brands` → `get_member_settings`、移除 `SELECT_BRANDS` action） |
| 2026-06-24 | `member_authorization` 拆分為 `member_authorize`（同意服務）與 `member_unauthorize`（解除服務）兩個獨立 endpoint；移除 `terms_version` 概念；新增 `get_member_authorization_logs`（後台，第二階段）說明 |
| 2026-06-24 | `campaign` 移除 `rotation_id` 直接外鍵；改以 `rotation_campaigns` 中間表管理多對多關聯；§4.2 欄位與 active 判斷邏輯同步更新；§二 Campaign 詞條規則同步更新 |
| 2026-06-24 | §二 名詞定義改寫為各詞條獨立區塊（概念 + 規則），涵蓋限制與關聯說明；§4.2 補充 `campaign.type` 不可變規則；`coupon` 新增 `rotation_id` 快照欄位說明 |
| 2026-06-24 | §4.2 Campaign 欄位：移除 `start_at`/`end_at`，新增 `type`（`auto`\|`manual`）與 `rotation_id`；active 判斷改由 rotation 繼承；說明 `type` 區分自動兌換與手動換券用途 |
| 2026-06-16 | 對齊 database-schema.md：`rotations` 的 `rotation_key` 改為 `id`（ULID），`display_coupon_min_order_amount`/`display_coupon_redeem_points` 合併為 `description`（JSON 字串）；`member_selected_brands` 使用 `rotation_id` FK；`member_brand_change_logs` 改為 request-level 模型（`change_brand` + JSON diffs）；API 對應改為 `get_current_rotation`/`get_member_settings` |
| 2026-06-16 | Coupon 狀態重新命名：`processing` → `consumed`、`completed` → `settled`；同步更新狀態機說明、訂單生命週期、各流程說明 |
| 2026-06-16 | `finalize_order` 改為批次接收（`orders` 陣列）、非同步處理，立即回 `202 Accepted` 並回傳 `request_id`；`request_id` 由發卡主機自行產生以支援重送冪等；新增 `get_finalize_batch_status` API 供查詢批次執行進度；新增 `finalize_batch_requests` / `finalize_batch_items` 資料表 |
| 2026-06-12 | 全域將 `user` 命名改為 `member`；新增 `member_authorization` 雙向授權機制；`members` 表加入 `auth_status`、`auth_updated_at` |

---

## 一、專案背景

原規劃方案「點數 + 金流整合支付（點加金）」因法規限制暫緩，本次改以「券加金」模式推動，即樹享券 2.0。

核心機制：用戶以點數兌換由神坊發行的「消費券（coupon）」，並於刷卡時即時折抵消費金額，在定性上趨近於消費代金，與一般禮票券有所區別。

## 二、名詞定義

### Rotation（輪播檔期）

#### 概念

定義活動起訖時間（`start_time` ～ `end_time`）、本檔期品牌選擇上限（`max_selectable_brand_count`）與前端顯示參數（`description`）。所有 campaign、用戶品牌選擇與 coupon 均以 rotation 為時間邊界。

#### 規則

1. 依 `start_time <= now() < end_time` 判斷是否為 active rotation
2. 系統同一時間只應有一個 active rotation；不同 rotation 的時間區間不得交集
3. 用戶的品牌選擇（`member_selected_brands`）與所選當下的 rotation 綁定，換檔後透過 lazy cleanup 自動失效
4. 不屬於當前 active rotation 的 campaign，一律視為非 active，無法觸發產券或兌換

---

### Brand（特店）

#### 概念

合作的品牌通路，亦稱為特店（例：全家、7-11、康是美）。brand 是 campaign 與 coupon 的頂層歸屬，也是用戶選擇自動兌換對象的單位。

#### 規則

1. 每個 brand 必須有唯一的 `treepoint_merchant_provider_key`，作為點數帳務通路識別，DB 不可為 null
2. 一個 brand 可跨多個 rotation 參與活動，每個 rotation 下可掛載多個 campaign
3. 同一 brand 同一時間只允許一個 `type = auto` 的 active campaign；`type = manual` 無此限制，可同時有多個 active

---

### Campaign（兌換活動）

#### 概念

定義特定 brand 在特定 rotation 內的產券規則，包含兌換門檻（`coupon_min_order_amount`）、所需點數（`coupon_redeem_points`）、折抵金額（`coupon_discount_amount`）與單筆上限（`max_redemptions_per_order`）。campaign 是 coupon 的發行依據。

#### 規則

1. campaign 本身不直接關聯 rotation；透過 `rotation_campaigns` 中間表掛載，支援同一 campaign 沿用於多個 rotation
2. 未掛載任何 rotation 的 campaign 不生效，但可提前建立備料；active 判斷為：`rotation_campaigns` 中是否存在對應當前 active rotation 的記錄
3. `type` 分為 `auto`（刷卡時自動觸發清算）與 `manual`（由用戶主動發起換券），一經建立不得更改
4. 同一 brand 同一時間只允許一個 `type = auto` 的 active campaign；`type = manual` 無此限制，可同時有多個 active
5. 兌換規則欄位（`coupon_min_order_amount`、`coupon_redeem_points`、`coupon_discount_amount`、`max_redemptions_per_order`）皆須大於 0
6. `member_selected_brands`（品牌預選清單）、lazy cleanup 與 `create_order` 清算機制僅服務 `type = auto` campaign；`type = manual` 的兌換由用戶主動發起，不受品牌預選清單限制，也不透過 `create_order` 觸發。`manual` 型目前僅有資料定義與 `get_current_rotation` 呈現規則，尚無對應的兌換執行 API/流程（詳見 `api_list.md` 待新增項目）

---

### Coupon（折抵券）

#### 概念

基於 campaign 產出、歸屬於特定用戶的折抵券 instance。每張 coupon 在建立時快照 campaign 的兌換規則與所在 rotation，確保後續統計與清算不受 campaign 事後變動影響。

#### 規則

1. coupon 建立時快照 `coupon_min_order_amount`、`coupon_redeem_points`、`coupon_discount_amount` 與 `rotation_id`，寫入後為唯讀
2. `expired_at` 於發券時一次計算寫死（`created_at 所在 UTC+8 日期 + coupon_valid_days` 之 `23:59:59.999`），不隨後續異動更新
3. 狀態機：`AVAILABLE → CONSUMED → SETTLED`；取消時 `CONSUMED → AVAILABLE`（未到期）或 `CONSUMED → EXPIRED`（已到期）
4. 狀態為 `CONSUMED` 的 coupon 不可納入下一筆交易的折抵計算

---

### Coupon Wallet（券夾）

#### 概念

用戶所有 coupon 的查詢視角，不是獨立資料表，而是對 `coupons` 表依 `member_id`、`brand_id`、`status` 組合查詢的投影。

#### 規則

1. 前端券夾首頁以品牌摘要呈現（`get_coupon_wallet`），點入品牌後才展示券列表（`get_coupons`）
2. 摘要顯示的 `available_coupon_count` 僅聚合 `status = AVAILABLE` 的券張數

---

### Point Balance（點數餘額）

#### 概念

用戶目前可用於兌換 coupon 的點數餘額，屬外部點數系統（TreePoint）管理，本系統不另設點數帳務表。

#### 規則

1. 點數在 `create_order` 時即時扣除，本系統不保存點數餘額
2. 訂單取消後點數不退還；券未到期則回 `AVAILABLE`，已到期則回 `EXPIRED`
3. 扣點時系統依訂單所屬 brand 的 `treepoint_merchant_provider_key` 作為通路識別

## 三、Coupon 狀態機

| 狀態 | 說明 | 可觸發的轉換 |
| --- | --- | --- |
| `available` | 可使用，存在於用戶券夾 | `consumed`（成立訂單）<br>`expired`（到期） |
| `consumed` | 交易授權中，等待商戶請款 | `settled`（請款完成）<br>`available`（取消且未到期）<br>`expired`（取消時已到期） |
| `settled` | 核銷完成，神坊代償完畢 | 終態 |
| `expired` | 超過有效期限自動過期 | 終態 |

> 注意：`consumed` 狀態的 `coupon` 在後續新訂單計算時不可再被使用，僅 `available` 的 `coupon` 可納入折抵計算。

## 四、Brand / Campaign 規則欄位

### 4.1 Brand 欄位

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `brand_id` | String | `brand` 主鍵（ULID），用於識別品牌主資料。 |
| `brand_name` | String | 品牌名稱，供前台顯示與後台維護使用。 |
| `brand_logo` | String | 品牌 Logo 資源位置或識別值，供前台品牌展示使用。 |
| `brand_category` | String | 品牌分類，用於品牌分組、顯示或營運管理。 |
| `treepoint_merchant_provider_key` | String | 該 `brand` 對應的 TreePoint 用點通路識別。執行 `create_order` 扣點時，系統需依 `brand` 讀取此欄位並帶入點數帳務流程，以區分點數是用於全家換券、7-11 換券等不同品牌場景。此欄位為品牌主資料必填，DB 不可為 `null`。 |
| `created_at` | DateTime | `brand` 建立時間（UTC+8 ISO 8601）。 |
| `updated_at` | DateTime | `brand` 最後更新時間（UTC+8 ISO 8601）。 |

### 4.2 Campaign 欄位

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `campaign_id` | String | `campaign` 主鍵（ULID）。 |
| `brand_id` | String | 對應 `brand` 主鍵（ULID），表示該 `campaign` 隸屬哪一個品牌。 |
| `type` | String | `auto`（系統自動兌換）\| `manual`（用戶手動兌換）。`auto` 型在刷卡時自動觸發清算；`manual` 型由用戶主動發起換券。**一經建立不得更改。** |
| `campaign_name` | String | `campaign` 名稱，供後台辨識與營運維護使用。 |
| `coupon_min_order_amount` | Integer | 【門檻】每消費滿 N 元，可對應使用一張 `coupon` |
| `coupon_redeem_points` | Integer | 兌換一張 `coupon` 所需點數 |
| `coupon_discount_amount` | Integer | 一張 `coupon` 可折抵的金額（元） |
| `max_redemptions_per_order` | Integer | 單筆交易中，當前 active campaign 最多可使用幾張券 |
| `created_at` | DateTime | `campaign` 建立時間（UTC+8 ISO 8601）。 |
| `updated_at` | DateTime | `campaign` 最後更新時間（UTC+8 ISO 8601）。 |

`campaign` 的 active 狀態由 `rotation_campaigns` 是否存在對應當前 active rotation 的記錄推導，`campaign` 本身不另設 `start_at` / `end_at`，也不直接持有 `rotation_id`。每一 `brand` 底下同一時間只允許一個 `type = auto` 的 active campaign；`type = manual` 無此限制。用戶需事先同意「刷卡時自動以點數兌換」，系統才會在刷卡時觸發清算。

> **現況提示：** `type = manual` 的 campaign 欄位定義與上述規則已確立，但目前僅止於資料模型與 `get_current_rotation` 的呈現（供前端「手動換券頁面」顯示規則），尚無對應的用戶主動兌換 API 或流程，屬於待補齊項目。

### 4.3 全域參數（system_configs）

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `coupon_valid_days` | Integer | 全域 coupon 有效天數設定，不屬於 `campaign` 欄位。發券時需以 `issued_at` 所在 UTC+8 日期加上此天數後，取當日 `23:59:59.999` 作為 `coupon.expired_at`。 |
> 注意：`brand_selection_limit` 已移至 `rotations` 表，由各檔期自行定義，詳見第十章「Rotation 輪播檔期」。

### 4.4 Rotation 欄位

| 欄位 | 類型 | 說明 |
| --- | --- | --- |
| `id` | String | 輪播檔期主鍵（ULID） |
| `start_time` | DateTime | 檔期開始時間（UTC+8 ISO 8601） |
| `end_time` | DateTime | 檔期結束時間（UTC+8 ISO 8601） |
| `max_selectable_brand_count` | Integer | 本檔期用戶最多可選品牌數量 |
| `description` | String | 前端顯示用說明參數，JSON 字串格式：`{"order_amount": N, "point_amount": N}`，由前端自行 parse 呈現 |

> 注意：`description` 目前不影響實際清算，清算依各品牌 `campaign` 的 `coupon_min_order_amount`、`coupon_redeem_points` 規則執行。

## 五、清算邏輯（create_order）

### 5.1 觸發條件

- 用戶已選擇該 `brand` 為自動兌換品牌
- 用戶已完成點數授權，且神坊系統可取得或驗證該授權結果
- 用戶已同意在此品牌刷卡時自動兌換
- 用戶的 `coupon_wallet` 有 `available` 的券，或 `point_balance > 0`

> 注意：即使 `point_balance = 0`，只要 `coupon_wallet` 仍有 `available` 的券，仍應觸發 `create_order`。

> 注意：若用戶在該 `brand` 下無任何 `available coupon`，且點數餘額也為 0，則 `create_order` 應直接失敗，回 `NO_AVAILABLE_COUPON_AND_POINT`。

### 5.2 計算步驟

1. 取出用戶在此 `brand` 下所有 `available coupon`，排除已過期券後，依下列順序排序（FIFO）：

   ```text
   expired_at ASC -> created_at ASC -> coupon_id ASC
   ```

2. 逐張檢查排序後的 `available coupon`：

   ```text
   若 剩餘消費額 >= coupon.coupon_min_order_amount，則使用該券
   若 剩餘消費額 < coupon.coupon_min_order_amount，則跳過該券，繼續檢查下一張
   ```

   每張券要嘛整張使用，要嘛不使用，不做部分用券。
   
   若該舊券 `campaign_id` 對應當前 active campaign，則需額外檢查本次已使用的 active-campaign 券數是否已達 `max_redemptions_per_order`；若已達上限，該舊券需跳過。

   若該舊券屬於歷史 campaign，則不受 `max_redemptions_per_order` 限制。

3. 計算「未被佔用消費額」：

   ```text
   剩餘消費額 = 原始消費額 - Σ（已使用券的 coupon_min_order_amount）
   ```

4. 在剩餘消費額上套用 active campaign：

   ```text
   可用張數 = 剩餘消費額 // coupon_min_order_amount
   ```

5. 先計算本次已使用的 active-campaign 舊券張數；再取 `remaining_active_campaign_quota = max_redemptions_per_order - active_campaign_coupon_used_count`，最後以 `min(可用張數, point_balance // coupon_redeem_points, remaining_active_campaign_quota)` 決定本次可再新發幾張 active-campaign 券。若 `remaining_active_campaign_quota <= 0`，則本次不得再新發任何 active-campaign 券。
6. 執行點數扣除，並即時派發對應張數的新 `coupon`（狀態為 `consumed`）。每張新券建立時即寫入固定 `expired_at`：

   ```text
   expired_at = (issued_at 所在 UTC+8 日期 + coupon_valid_days) 的 23:59:59.999
   ```

7. 所有被使用的既有 `available coupon` 狀態改為 `consumed`。
8. 回傳本次折抵總金額：

   ```text
   本次折抵總金額 = Σ（本次所有 processing coupon 的 coupon_discount_amount）
   ```

`create_order` response 僅需回傳 `discount_amount`。若呼叫方或前台端需查詢訂單完整資訊、用券明細、事件歷程或卡號後四碼，應另呼叫 `get_order`。

### 5.3 create_order 成功定義

- `create_order` 僅在同一個 DB transaction 內完成以下動作後，才視為正式成功建立：
  - 完成點數扣除
  - 完成新券派發
  - 完成既有 `available coupon` 轉為 `consumed`
  - 完成 order 建立
  - 完成 `order_logs` 建立
- 任一環節失敗，整筆 transaction 應 rollback，該次 `create_order` 視為訂單未成功建立。
- 執行點數扣除時，系統應依訂單所屬 `brand` 讀取其 `treepoint_merchant_provider_key`，作為點數帳務通路識別。
- order 僅保存最終 `brand_id`，不額外保存其他門店層原始交易識別欄位。

### 5.4 計算範例

情境設定：

| 項目 | 數值 |
| --- | --- |
| 訂單金額（`cash_amount`） | 620 元 |
| 用戶 `point_balance` | 26 點 |
| active campaign | 滿 100 折 21：`coupon_min_order_amount=100`、`coupon_redeem_points=20`、`coupon_discount_amount=21`、`max_redemptions_per_order=3` |
| `coupon_wallet` 中既有券 | `coupon_01`（old_campaign）：`coupon_min_order_amount=400`、`coupon_discount_amount=120` |

計算過程：

1. FIFO 取出 `coupon_01`（唯一 `available` 券）。
2. 計算剩餘消費額：

   ```text
   620 // 400 = 1
   coupon_01 可使用 1 張（剛好符合門檻）

   剩餘消費額 = 620 - 400 = 220
   ```

3. 套用 active campaign：

   ```text
   220 // 100 = 2
   理論可再用 2 張
   ```

4. 確認點數是否足夠：

   ```text
   26 // 20 = 1
   點數只夠換 1 張

   campaign 上限為 3 張

   min(2, 1, 3) = 1
   ```

5. 扣點 20，派發 `coupon_02`（`consumed`）。
6. `coupon_01` 狀態改為 `consumed`。
7. 折抵總額：

   ```text
   120 + 21 = 141 元
   ```

### 5.5 帳務流程

1. 用戶授權商戶刷卡 620 元。
2. 神坊扣除用戶點數 20 點。
3. 商戶向銀行請款 620 元。
4. 神坊確認核銷 `coupon_01` + `coupon_02`。
5. 神坊代償 141 元給銀行。
6. 用戶實際應繳卡費：`620 - 141 = 479` 元。

### 5.6 重複請求處理規則

- `create_order` 以 `order_id` 作為唯一訂單識別。
- 同一 `order_id` 已成功建立後，任何再次收到的 `create_order` 請求，一律回 `ORDER_ALREADY_EXIST`。
- 不區分再次收到的請求內容是否與首次一致；即使 `brand`、`cash_amount` 或其他欄位不同，仍回 `ORDER_ALREADY_EXIST`。
- 重複建立不得再次扣點、發券、改券狀態或新增事件。
- 首次 `create_order` 成功後，訂單即進入 `PROCESSING` 狀態，等待後續 `finalize_order`。

## 六、訂單生命週期

| 事件 | 觸發方 | Coupon 狀態變化 | 點數 |
| --- | --- | --- | --- |
| `create_order`<br>（刷卡授權後） | 發卡主機 | `available` → `consumed` | 即時扣除 |
| `finalize_order`<br>（商戶請款完成） | 發卡主機 | `consumed` → `settled` | 不異動 |
| `finalize_order`<br>（商戶取消交易） | 發卡主機 | `consumed` → `available`（未到期）<br>`consumed` → `expired`（已到期） | 不退還 |

> 注意：`create_order` 與 `finalize_order` 為非同步，兩者之間可能有數天時間差。`consumed` 期間的券不可再被計入下一筆交易的折抵計算。

> 注意：取消交易後，本次交易即時兌換產生的新券（如 `coupon_02`）若在 finalize 當下尚未到期，則回 `available`；若已到期，則回 `expired`。點數不返還。

### 6.1 訂單狀態

| order_status | 說明 |
| --- | --- |
| `PROCESSING` | 已完成 `create_order`，等待商戶請款或取消交易結果 |
| `COMPLETED` | 已收到請款完成的 `finalize_order`，訂單結案 |
| `CANCELLED` | 已收到取消交易的 `finalize_order`，訂單結案 |

- `PROCESSING` 是唯一可執行 `finalize_order` 的狀態。
- `COMPLETED`、`CANCELLED` 為終態，進入後不得再次轉換。

### 6.2 finalize_order 批次與非同步機制

- `finalize_order` 採批次接收：發卡主機一次可傳入多筆 `{order_id, action}`，神坊立即回 `202 Accepted`，實際處理以非同步方式執行。
- 發卡主機自行產生 `request_id` 並帶入請求；重送相同 `request_id` 時，神坊直接回傳原批次接收資訊，不重複建立或執行。
- 發卡主機可透過 `get_finalize_batch_status` 查詢各筆訂單的處理進度。
- 單筆處理失敗（`ORDER_NOT_FOUND`、`ORDER_ALREADY_FINALIZED`）不中斷整批次，失敗原因記錄於該筆的 `error_code`。

### 6.3 重複 finalize 規則

- 同一 `order_id` 已完成最終化後，再次被納入批次時，該筆 item 標記為 `FAILED`，`error_code = ORDER_ALREADY_FINALIZED`。
- 不區分再次收到的是相同結果或不同結果；已 `COMPLETED` 後收到 `CANCELLED`，或已 `CANCELLED` 後收到 `COMPLETED`，皆以 `ORDER_ALREADY_FINALIZED` 記錄失敗。
- 重複 finalize 不得再次改變訂單狀態、券狀態、點數或新增事件。

## 七、資訊流程

### Flow 1：品牌一覽

用戶可瀏覽所有合作品牌及對應的當期自動兌換規則。

需回傳欄位：

- `max_selectable_brand_count`
- `brand_name`
- `brand_logo`
- `brand_category`
- `brand_active_campaign_details`（`coupon_min_order_amount`、`coupon_redeem_points`、`coupon_discount_amount`、`max_redemptions_per_order`）

### Flow 2：用戶授權

用戶需先同意樹享券平台可使用其點數，作為後續品牌設定與 `create_order` 用點清算的前置條件。授權操作分為兩個獨立 endpoint：

**同意服務（`activate_member`，原 `member_authorize`）**
- 用戶在 CR 前台首次同意或重新同意樹享券服務
- 神坊呼叫點數系統 API，兩邊皆成功後更新 `members.auth_status = AUTHORIZED`，寫入一筆 `member_authorization_logs`（action=AUTHORIZE）；API 對外回傳 `status: ACTIVE`
- 冪等：已為 `AUTHORIZED` 時重複呼叫不重複寫 log

**解除服務（`deactivate_member`，原 `member_unauthorize`）**
- 用戶在 CR 前台主動解除服務
- 神坊更新 `members.auth_status = DEAUTHORIZED`，寫入一筆 `member_authorization_logs`（action=DEAUTHORIZE）；API 對外回傳 `status: INACTIVE`
- 解除後 `AVAILABLE` coupon 保留但不可用於新交易；`CONSUMED` 狀態的 order 繼續走完原流程
- 冪等：已為 `DEAUTHORIZED` 時重複呼叫不重複寫 log

**授權歷史**
- 完整異動歷史保存於 `member_authorization_logs`，未來透過 `/coupon/admin/get_member_authorization_logs` 供客服查詢（第二階段）

### Flow 3：選擇品牌

用戶選擇偏好的 `brand` 加入自動兌換清單。

系統需驗證：

- 用戶是否已完成點數授權。
- 用戶已選品牌數量：不超過當前 active rotation 的 `max_selectable_brand_count`。
- 所選 `brand` 是否有 active campaign。

### Flow 4：瀏覽已選品牌

顯示用戶目前的品牌設定狀態，包含服務是否啟用，以及已選擇的品牌 ID 清單。

需回傳欄位：

- `auto_redeem_enabled`
- `last_brand_selection_changed_at`
- `selected_brand_ids`：已選且當前仍具備 active auto campaign 的品牌 id 清單

> 前端如需品牌名稱、logo、campaign 詳情，應另呼叫 `get_current_rotation`；`max_selectable_brand_count` 亦由 `get_current_rotation` 取得。

若用戶處於暫停用券狀態，仍可回傳目前已選且當前有 active campaign 的品牌 id 清單，但 `auto_redeem_enabled = false`。

`last_brand_selection_changed_at` 代表該用戶品牌選擇狀態最近一次異動時間，僅於首次選牌、更換品牌、lazy cleanup 換檔清空（值為舊 rotation 的 `end_time`）時更新；`PAUSE` / `RESUME` 不更新此欄位。

### Flow 5：異動紀錄

調閱用戶過往 1 年內的品牌選擇異動紀錄，包含：

- 異動時間
- 異動行為（首次啟用、暫停用券、重啟用券、更換品牌）
- 更換品牌需記錄「更換前的品牌清單」vs「更換後的品牌清單」

底層資料模型採 `member_brand_change_logs` request 粒度事件模型：

- 每次品牌異動或服務狀態切換各寫入**一筆**紀錄
- `type` enum：`change_selected_brands` | `system_clear_brands` | `disable_auto_redeem` | `enable_auto_redeem`
- `data` 欄位（nullable JSON），儲存**快照**（非 diff），可直接作為 API 回傳值：
  - `type = change_selected_brands`（首次選牌與後續換牌）：格式 `{"before_brands": [{"id": "...", "name": "..."}], "after_brands": [{"id": "...", "name": "..."}]}`；首次選牌時 `before_brands = []`
  - `type = system_clear_brands`（lazy cleanup 清空）：`{"before_brands": [被清除品牌快照], "after_brands": []}`；`created_at` 設為舊 rotation 的 `end_time`
  - `type = disable_auto_redeem` / `enable_auto_redeem`（暫停／啟用服務）：`data = null`
- `members.auto_redeem_enabled` 記錄當前狀態；`member_brand_change_logs` 同時保存完整歷程（含 pause/resume）

### Flow 5-1：Rotation 換檔後自動清空已選品牌（Lazy Cleanup）

系統採用「用戶進入時觸發」的 lazy cleanup 機制取代批次清除，不需要額外的 batch job。

規則如下：

- 觸發點：用戶呼叫 `get_member_settings`、`update_member_selected_brands`、`create_order` 等需讀取已選品牌的 API 時自動執行
- 判斷條件：比對 `member_selected_brands.rotation_id` 與當前 active rotation 的 `id`，若不符則代表舊檔期選擇已失效
- 清除動作：
  - 刪除該用戶在 `member_selected_brands` 中的所有舊檔期記錄
  - 在 `member_brand_change_logs` 寫入一筆 `system_clear_brands` 事件：`data = {"before_brands": [被清除品牌快照], "after_brands": []}`，`created_at` 設為舊 rotation 的 `end_time`
- `auto_redeem_enabled` 保留原值不受影響
- 冪等性：若已選品牌已為空，skip，不重複寫入 change log
- `last_brand_selection_changed_at` 需更新為舊 rotation 的 `end_time`（與 change log 的 `created_at` 一致）

### Flow 6：券夾

顯示用戶券夾列表，預設回全部券狀態，並可依品牌或券狀態篩選。

需回傳欄位：

- `coupon_id`
- `status`
- `brand_id`
- `brand_name`
- `brand_logo`
- `campaign_id`
- `campaign_name`
- `coupon_min_order_amount`
- `coupon_redeem_points`
- `coupon_discount_amount`
- `max_redemptions_per_order`
- `expired_at`

### Flow 7：刷卡

> 本節僅涵蓋 `type = auto` 的自動兌換流程；`type = manual` 的用戶主動兌換流程尚未定義，詳見 `api_list.md` 待新增項目。

子流程 7a：`create_order`（信用卡授權後同步執行）

- 由發卡主機呼叫，帶入 `brand`、用戶識別、消費金額、本次刷卡卡號後四碼，以及刷卡門市名稱（`merchant_name`）。
- 神坊執行第五章清算邏輯，僅回傳折抵金額 `discount_amount`。
- 若需查詢訂單完整資訊，另以同一 `order_id` 呼叫 `get_order`。
- 若同一 `order_id` 再次建立，一律回 `ORDER_ALREADY_EXIST`，不重做清算。

訂單底層資料模型包含：

- `order_logs`：保存 `CREATED`、`COMPLETED`、`CANCELLED` 等訂單歷程事件
- `order_coupon_logs`：保存本次訂單實際使用的券明細快照

> 卡號後四碼僅作為訂單顯示資訊保存，不參與任何品牌識別、點數清算或折抵計算邏輯。

子流程 7b：`finalize_order`（商戶請款或取消交易後批次非同步執行）

- 發卡主機以批次方式傳入多筆 `{order_id, action}`，神坊立即回 `202 Accepted` 並回傳 `request_id`。
- 請款成功（`action = COMPLETED`）：`coupon` 狀態 `consumed` → `settled`，神坊代償。
- 取消交易（`action = CANCELLED`）：`coupon` 狀態依是否已到期轉為 `available` 或 `expired`，點數不退還。
- 若該 `order_id` 已完成最終化，該筆記錄失敗（`error_code = ORDER_ALREADY_FINALIZED`），不影響同批次其他筆。
- 發卡主機可透過 `get_finalize_batch_status` 輪詢批次執行進度。

## 八、角色與系統邊界

| 角色 | 說明 |
| --- | --- |
| 神坊（票券發行方） | 負責 `coupon` 發行、點數清算、代償銀行 |
| 發卡主機（銀行） | 觸發 `create_order` 與 `finalize_order`，為主要呼叫方 |
| 品牌通路（特店） | 刷卡場域，向銀行請款或申請取消交易 |
| 樹享券平台前台端 | 用戶介面，顯示品牌清單、券夾、異動紀錄 |

## 九、商務規則彙整（環境參數）

| 規則 | 預設值 | 備註 |
| --- | --- | --- |
| 每人最多選擇品牌數 | 由 rotation 定義 | 各檔期可設定不同上限，取自 `rotations.max_selectable_brand_count` |
| 品牌更換頻率 | 不限次數（由 frontend 管控） | 後端不做次數限制 |
| Coupon 使用順序 | FIFO（先到期先用） | 依 `expired_at`、`created_at`、`coupon_id` 排序；不符合門檻的券跳過 |
| 點數不足時 | 只換點數足夠支付的張數 | 不部分扣點 |
| 取消交易時點數 | 不返還 | 券未到期回 `available`；已到期回 `expired` |
| `consumed` 券可用性 | 不可納入下一筆交易計算 | 視同已用 |
| `coupon_valid_days` | 全域參數 | 發券時計算 `expired_at = (issued_at 所在 UTC+8 日期 + coupon_valid_days) 的 23:59:59.999` |

## 十、Rotation 輪播檔期

### 10.1 概念說明

Rotation 是樹享券 2.0 的活動檔期機制。每個 rotation 定義一段活動時間（`start_time` ～ `end_time`），並攜帶本檔期的品牌選擇上限與前端顯示參數。

用戶在某一 rotation 期間設定的品牌選擇，與該 `rotation_id` 綁定。換檔後，舊選擇自動視為失效，用戶需重新設定。

系統同一時間只應有一個 active rotation（`start_time <= now() < end_time`）。

### 10.2 用戶品牌選擇與 Rotation 的綁定

用戶執行 `update_member_selected_brands` 時，以 `brand_ids` 作為更新後完整清單，系統計算 diff 後覆寫 `member_selected_brands`，並將當下 active rotation 的 `id` 寫入 `member_selected_brands.rotation_id`。

- 若當下無 active rotation，拒絕操作，回傳 `NO_ACTIVE_ROTATION`
- 每次執行均覆寫 `rotation_id` 為當前 active rotation 的 id

### 10.3 換檔後的 Lazy Cleanup 機制

系統不以批次清除方式處理換檔，而是在用戶進入系統時自動觸發 lazy cleanup：

**觸發點：**
- `get_member_settings`
- `update_member_selected_brands`
- `create_order`

**判斷邏輯：**
```
if member_selected_brands.rotation_id != current_active_rotation.id:
    → 視為舊檔期選擇，執行清除
```

**清除行為（同一 DB transaction）：**
1. 刪除該用戶在 `member_selected_brands` 中的所有舊檔期記錄
2. 在 `member_brand_change_logs` 寫入一筆 `system_clear_brands` 事件：`data = {"before_brands": [被清除品牌快照], "after_brands": []}`，`created_at`：舊 rotation 的 `end_time`（語意時間點）

**冪等性：** 若已選清單已為空，skip，不重複寫入。

### 10.4 前端顯示參數

`description` 儲存於 `rotations` 表，透過 `get_current_rotation` API 供前端取得，用於顯示活動說明文字（例如「每消費 100 元可折抵 10 點」），格式為 JSON 字串 `{"order_amount": N, "point_amount": N}`，由前端自行 parse 呈現。

**重要：** `description` 目前不影響實際清算邏輯。清算時，系統依各品牌 `campaign` 的 `coupon_min_order_amount` 與 `coupon_redeem_points` 執行。

### 10.5 API 對應

| API | Rotation 相關行為 |
| --- | --- |
| `get_current_rotation` | 回傳當前 active rotation 的完整設定及品牌清單 |
| `get_member_settings` | 觸發 lazy cleanup；`max_selectable_brand_count` 取自 active rotation |
| `update_member_selected_brands` | 觸發 lazy cleanup；以 `brand_ids` 全量覆寫並寫入當前 `rotation_id`；驗證品牌數上限使用 active rotation 的 `max_selectable_brand_count` |
| `create_order` | 觸發 lazy cleanup；若清除後品牌不在清單中，回 `AUTO_REDEEM_NOT_ENABLED_FOR_BRAND` |

---

文件版本：1.0
建立日期：2026/06/06
發行單位：神坊
